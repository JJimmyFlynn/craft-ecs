name: Build and Push Container Image
on:
  workflow_dispatch:
  push:
    branches:
      - main
permissions:
  id-token: write
  contents: read
jobs:
  BuildImage:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: arn:aws:iam::504200660083:role/githubOIDCRole
          aws-region: us-east-1
      - name: Login to Amazon ECR
        uses: docker/login-action@v2
        with:
          registry: docker/login-action@v2
      - name: Build, tag, and push docker image to Amazon ECR
        uses: docker/build-push-action@v4
        env:
          REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          REPOSITORY: craft-europa
          IMAGE_TAG: ${{ github.sha }}
        with:
          push: true
          tags: $REGISTRY/$REPOSITORY:$IMAGE_TAG,$REGISTRY/$REPOSITORY:latest
#      - name: Start ECS Deployment
#        run: aws ecs update-service --cluster Craft_ECS --service craft_web --force-new-deployment


